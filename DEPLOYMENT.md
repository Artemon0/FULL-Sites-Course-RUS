# 🚀 Деплой курса на GitHub Pages

## Автоматический деплой

Курс автоматически публикуется на GitHub Pages при каждом push в ветку `main`.

### Настройка GitHub Pages

1. **Форкните репозиторий** или создайте свой
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите:
   - Source: **GitHub Actions**
4. Сохраните изменения

### GitHub Actions

Файл `.github/workflows/deploy.yml` автоматически:
- Собирает сайт из папки `docs/`
- Публикует на GitHub Pages
- Обновляется при каждом push

### Проверка деплоя

После push в `main`:
1. Перейдите во вкладку **Actions**
2. Дождитесь завершения workflow (зелёная галочка)
3. Откройте сайт: `https://yourusername.github.io/repository-name/`

## Локальный запуск

### Вариант 1: Live Server (VS Code)

1. Установите расширение **Live Server**
2. Откройте `docs/index.html`
3. Нажмите **Go Live** в статус-баре

### Вариант 2: Python

```bash
cd docs
python -m http.server 8000
```

Откройте: http://localhost:8000

### Вариант 3: Node.js

```bash
npx serve docs
```

## Структура проекта

```
📁 html-css-js-course/
├── 📁 docs/                    # Веб-сайт курса
│   ├── index.html              # Главная страница
│   ├── modules.html            # Страница модулей
│   ├── 📁 css/                 # Стили
│   └── 📁 js/                  # JavaScript
├── 📁 module-1-html/           # Материалы модулей
├── 📁 module-2-css/
├── ...
└── 📁 .github/
    └── 📁 workflows/
        └── deploy.yml          # GitHub Actions
```

## Кастомизация

### Изменить URL в README

Замените `yourusername` на ваш GitHub username:
```markdown
https://yourusername.github.io/html-css-js-course/
```

### Изменить дизайн

Отредактируйте файлы в `docs/css/`:
- `style.css` - основные стили
- `modules-page.css` - стили страницы модулей

### Добавить модули

Отредактируйте `docs/js/modules-data.js`:
```javascript
const modulesData = [
    {
        id: 7,
        icon: '🎯',
        number: 'Модуль 7',
        title: 'Новый модуль',
        // ...
    }
];
```

## Проблемы и решения

### Сайт не открывается

1. Проверьте, что GitHub Actions завершился успешно
2. Убедитесь, что в Settings → Pages выбран **GitHub Actions**
3. Подождите 5-10 минут после первого деплоя

### Стили не применяются

1. Проверьте пути к CSS файлам
2. Очистите кэш браузера (Ctrl+Shift+R)
3. Проверьте консоль браузера на ошибки

### Редактор кода не работает

1. Проверьте подключение CodeMirror CDN
2. Откройте консоль браузера для ошибок
3. Убедитесь, что JavaScript файлы загружаются

## Обновление курса

1. Внесите изменения в файлы
2. Commit и push в `main`:
   ```bash
   git add .
   git commit -m "Обновление курса"
   git push origin main
   ```
3. GitHub Actions автоматически обновит сайт

## Дополнительные возможности

### Кастомный домен

1. Купите домен
2. В Settings → Pages → Custom domain введите домен
3. Настройте DNS записи у регистратора

### Аналитика

Добавьте Google Analytics в `docs/index.html`:
```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

## Поддержка

Если возникли проблемы:
1. Проверьте [Issues](https://github.com/yourusername/html-css-js-course/issues)
2. Создайте новый Issue с описанием проблемы
3. Посмотрите логи в GitHub Actions

---

**Удачи с деплоем! 🎉**

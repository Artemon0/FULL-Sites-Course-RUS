# 🌐 Веб-сайт курса

Это интерактивный веб-сайт для изучения курса HTML + CSS + JavaScript.

## Возможности

### 🎓 Интерактивное обучение
- Просмотр всех модулей курса
- Навигация по материалам
- Отслеживание прогресса

### 💻 Встроенный редактор кода
- Редактор HTML, CSS и JavaScript
- Подсветка синтаксиса (CodeMirror)
- Мгновенный предпросмотр результата
- Примеры кода для начала

### 📱 Адаптивный дизайн
- Работает на всех устройствах
- Оптимизирован для мобильных
- Современный UI/UX

## Технологии

- **HTML5** - структура
- **CSS3** - стилизация
- **JavaScript** - интерактивность
- **CodeMirror** - редактор кода

## Локальный запуск

### Вариант 1: Открыть файл
Просто откройте `index.html` в браузере

### Вариант 2: Локальный сервер

**Python:**
```bash
python -m http.server 8000
```

**Node.js:**
```bash
npx serve
```

**PHP:**
```bash
php -S localhost:8000
```

Затем откройте: http://localhost:8000

## Структура

```
docs/
├── index.html          # Главная страница
├── modules.html        # Страница модулей
├── css/
│   ├── style.css       # Основные стили
│   └── modules-page.css # Стили страницы модулей
└── js/
    ├── main.js         # Главный скрипт
    ├── editor.js       # Редактор кода
    └── modules-data.js # Данные модулей
```

## Кастомизация

### Изменить цвета

Отредактируйте CSS переменные в `css/style.css`:
```css
:root {
    --primary: #2563eb;
    --secondary: #64748b;
    /* ... */
}
```

### Добавить модуль

Отредактируйте `js/modules-data.js`:
```javascript
const modulesData = [
    {
        id: 7,
        icon: '🎯',
        title: 'Новый модуль',
        // ...
    }
];
```

### Изменить примеры в редакторе

Отредактируйте функцию `setExampleCode()` в `js/editor.js`

## Деплой

Сайт автоматически публикуется на GitHub Pages через GitHub Actions.

Подробнее: [DEPLOYMENT.md](../DEPLOYMENT.md)

## Браузеры

Поддерживаются современные браузеры:
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## Лицензия

Открытый исходный код. Используйте свободно!

---

**Приятного обучения! 🚀**
